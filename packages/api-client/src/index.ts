// Public surface of the generated API client: OpenAPI-derived types only.
// Runtime fetch logic lives in the consuming app (server-side), using these types.
export type { components, operations, paths } from "./generated/schema";
